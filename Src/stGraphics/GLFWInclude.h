#pragma once

//glew must always be included before glfw in order to get the latest gl libraries
//to enforce this, should always just include this header if you need glfw

#include "GL/glew.h"
#include "GLFW/glfw3.h"
